services:
  - type: web
    name: baileys-whatsapp-server
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: sessions
      mountPath: /app/sessions
      sizeGB: 1
